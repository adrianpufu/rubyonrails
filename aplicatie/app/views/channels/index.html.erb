<h1>Listing channels</h1>
 
<table>
  <tr>
    <th>Name</th>
  </tr>
 
  <% @channels.each do |channel| %>
    <tr>
      <td><%= channel.name %></td>
      <td><%= link_to 'Show', channel_path(channel) %></td>
    </tr>
  <% end %>
</table>